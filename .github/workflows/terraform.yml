name: Terraform Infrastructure

on:
  push:
    branches: [main]
    paths:
      - 'terraform/**'
      - 'infra/terraform/**'
  pull_request:
    branches: [main]
    paths:
      - 'terraform/**'
      - 'infra/terraform/**'
  workflow_dispatch:

env:
  TF_CLOUD_ORGANIZATION: "your-org-name"
  TF_WORKSPACE: "devops-pipeline-infrastructure"

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./infra/terraform
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -no-color
        continue-on-error: true

  terraform-security:
    name: Terraform Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tfsec
        uses: aquasecurity/tfsec-action@v1.0.0
        with:
          working_directory: infra/terraform
          soft_fail: true

      - name: Run Checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: infra/terraform
          framework: terraform
          soft_fail: true

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [terraform-plan, terraform-security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
    defaults:
      run:
        working-directory: ./infra/terraform
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
          terraform_wrapper: false

      - name: Terraform Init
        run: terraform init

      - name: Terraform Apply
        run: terraform apply -auto-approve

      - name: Extract Terraform Outputs
        id: tf-outputs
        run: |
          echo "vm_ip=$(terraform output -raw vm_public_ip || echo 'N/A')" >> $GITHUB_OUTPUT
          echo "acr_name=$(terraform output -raw acr_name || echo 'N/A')" >> $GITHUB_OUTPUT
          echo "acr_server=$(terraform output -raw acr_login_server || echo 'N/A')" >> $GITHUB_OUTPUT

      - name: Save outputs as artifacts
        uses: actions/upload-artifact@v3
        with:
          name: terraform-outputs
          path: infra/terraform/terraform-outputs.json
